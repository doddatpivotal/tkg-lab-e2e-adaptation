resources:
- name: source-code
  type: git
  source:
    uri: ((petclinic.codeRepo))
    branch: main
- name: config-repo
  type: git
  source:
    uri: ((petclinic.configRepo))
    branch: master
    paths:
     - "k8s/**"
- name: spring-petclinic-image
  type: docker-image
  source:
    repository: ((petclinic.image))
    tag: latest

jobs:
- name: continuous-integration
  plan:
    - get: source-code
      trigger: true
    - task: compile-and-test
      output_mapping:
        target: target
      config:
        platform: linux
        image_resource:
          type: docker-image
          source:
            repository: mcr.microsoft.com/dotnet/core/sdk:3.1
            tag: 3.1
        inputs:
          - name: source-code
        outputs:
          - name: target
        # caches:
        #   - path: source-code/maven
        run:
          path: /bin/bash
          args:
            - -c
            - |
              cd source-code
              # if [[ -d $PWD/maven && ! -d $HOME/.m2 ]]; then
              #   ln -s "$PWD/maven" "$HOME/.m2"
              # fi
              # Added -DskipTests and -Dcheckstyle.skip to speed up task for demo purpose
              # They should not be included in a proper test pipeline
              dotnet build employee-todo-list-api
              cp employee-todo-list-api/bin/Debug/netcoreapp3.1/*.dll ../target
    - task: update-build-service-image
      params:
        KUBECONFIG_JSON: ((common-secrets.kubeconfigBuildServer))
      input_mapping:
        target: target
      config:
        platform: linux
        image_resource:
          type: docker-image
          source:
            repository: ((common-secrets.concourseHelperImage))
            tag: latest
        inputs:
          - name: target
        run:
          path: /bin/bash
          args:
            - -c
            - |
              docker login ((common-secrets.harborDomain)) -u ((common-secrets.harborUser)) -p ((common-secrets.harborPassword))
              echo $KUBECONFIG_JSON>kubeconfig.json
              export KUBECONFIG=kubeconfig.json
              set +e
              kp image list -n ((todos.tbsNamespace)) | grep "todos" 
              exists=$?
              set -e
              if [ $exists -eq 0 ]; then
                kp image patch todos \
                  --namespace ((todos.tbsNamespace)) \
                  --wait \
                  --local-path target/employee-todo-list-api.dll
              else
                kp image create chinookapi \
                  --tag harbor.tanzu-poc.winterfell.live/sandbox/chinookapi \
                  --namespace tbs-project-sandbox \
                  --local-path /mnt/c/dev/Chinnook/employee-todo-list-api.dll \
                  --wait \
              fi


- name: continuous-deployment
  public: true
  serial: true
  plan:
    - get: spring-petclinic-image
      trigger: true
    - get: config-repo
    - task: create-wavefront-event
      params:
        WAVEFRONT_API_TOKEN: ((todos.wavefrontApiToken))
        WAVEFRONT_URL: ((todos.wavefrontUri))
        WAVEFRONT_DEPLOY_EVENT_NAME: ((todos.wavefrontDeployEventName))
      config:
        platform: linux
        image_resource:
          type: docker-image
          source:
            repository: ((common-secrets.concourseHelperImage))
            tag: latest
        run:
          path: /bin/bash
          args:
            - -c
            - |
              set -euo pipefail

              START_TIME=$(date +%s000)
              sleep 1
              END_TIME=$(date +%s000)

              curl \
                -X POST \
                --header "Content-Type: application/json" \
                --header "Accept: application/json" \
                --header "Authorization: Bearer ${WAVEFRONT_API_TOKEN}" \
                -d "{
                  \"name\": \"${WAVEFRONT_DEPLOY_EVENT_NAME}\",
                  \"annotations\": {
                    \"severity\": \"info\",
                    \"type\": \"image deploy\",
                    \"details\": \"new todos image deployed\"
                  },
                  \"startTime\": "${START_TIME}",
                  \"endTime\": "${END_TIME}"
                }" "${WAVEFRONT_URL}/api/v2/event"

    - task: deploy-app
      params:
        KUBECONFIG_JSON: ((common-secrets.kubeconfigAppServer))
      config:
        platform: linux
        image_resource:
          type: docker-image
          source:
            repository: ((common-secrets.concourseHelperImage))
            tag: latest
        inputs:
          - name: config-repo
          - name: spring-petclinic-image
        run:
          path: /bin/bash
          args:
            - -c
            - |
              export DIGEST=$(cat spring-petclinic-image/digest)

              # TODO Need to setup the kubeconfig
              echo $KUBECONFIG_JSON>kubeconfig.json
              export KUBECONFIG=kubeconfig.json

              cat > config-repo/k8s/values.yml << EOF
              #@data/values
              ---
              petclinic:
                host: ((petclinic.host))
                image: ((petclinic.image))@$DIGEST
                wavefront:
                  applicationName: ((petclinic.wavefrontApplicationName))
                  uri: ((petclinic.wavefrontUri))
                  apiToken: ((petclinic.wavefrontApiToken))
              EOF
              cat config-repo/k8s/values.yml
              
              ytt -f config-repo/k8s --ignore-unknown-comments | kapp deploy -n petclinic -a petclinic -y -f -


